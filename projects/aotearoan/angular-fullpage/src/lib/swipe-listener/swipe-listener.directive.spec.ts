import { SwipeListenerDirective } from './swipe-listener.directive';

describe('SwipeListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeListenerDirective();
    expect(directive).toBeTruthy();
  });
});
